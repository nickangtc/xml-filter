{
  "name": "filter-xml",
  "version": "1.0.0",
  "description": "This is a tool that parses every single `<product>` node (can be named differently) and outputs only those that match a list of IDs.",
  "main": "start.js",
  "dependencies": {},
  "devDependencies": {
    "commander": "^2.16.0",
    "inquirer": "^6.0.0",
    "jsdom": "^11.12.0",
    "xml-reader": "^2.4.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
